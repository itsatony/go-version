# go-version Environment Variables Example
# Copy this file to .env and adjust values as needed

# Development settings
GO_VERSION_DEV_MODE=true
GO_VERSION_LOG_LEVEL=debug

# Manifest path (defaults to versions.yaml in current directory)
# GO_VERSION_MANIFEST_PATH=./versions.yaml

# HTTP server settings (for examples)
# GO_VERSION_HTTP_PORT=8080
# GO_VERSION_HTTP_HOST=localhost

# Logging format (json or console)
# GO_VERSION_LOG_FORMAT=console
